<UserControl x:Class="UDSH.View.HeaderUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UDSH.View"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>

        <!-- Logo Placement -->
        <Rectangle Grid.Column="0" Fill="#FFADADAD" Panel.ZIndex="0"/>
        <Image Grid.Column="0" Panel.ZIndex="1" Source="/Resource/Logo.png" RenderOptions.BitmapScalingMode="HighQuality" Margin="5"/>

        <!-- Titles -->
        <Rectangle Grid.Column="1" Fill="#CDCDCD"/>
        <TextBlock Name="ProjectName" Grid.Column="1" Text="Monster" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,0,0,0" FontSize="20"/>


        <Grid Grid.Column="0" Grid.ColumnSpan="3" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <!-- Emoji and flags placeholders -->
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="UDSH" VerticalAlignment="Center" Margin="0,0,0,0" FontSize="20"/>
            <TextBlock Grid.Column="1" Text="|" VerticalAlignment="Center" Margin="5,0,5,5" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock Grid.Column="2" Text="Mohammed Kenawy" VerticalAlignment="Center" Margin="0,0,0,0" FontSize="20"/>

            <TextBlock Grid.Column="3" Text="𓀀" VerticalAlignment="Center" Margin="0,0,0,0" FontSize="20"/>
            <TextBlock Grid.Column="4" Text="𓀀" VerticalAlignment="Center" Margin="0,0,0,0" FontSize="20"/>
            <TextBlock Grid.Column="5" Text="𓀀" VerticalAlignment="Center" Margin="0,0,0,0" FontSize="20"/>
        </Grid>

        <!-- Page Controls -->
        <Grid Grid.Column="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Rectangle Grid.Column="0" Panel.ZIndex="0"/>
            <Button Grid.Column="0" Panel.ZIndex="1" Click="MinimizeButton"/>
            <Image Grid.Column="0" Panel.ZIndex="2" Source="/Resource/Minimize.png" Margin="16,13,16,13" IsHitTestVisible="False"/>

            <Rectangle Grid.Column="1" Panel.ZIndex="0"/>
            <Button Grid.Column="1" Panel.ZIndex="1" Click="MaximizeButton"/>
            <Image Grid.Column="1" Panel.ZIndex="2" Source="/Resource/Normal.png" Margin="15,13,15,13" IsHitTestVisible="False"/>

            <Rectangle Grid.Column="2" Panel.ZIndex="0"/>
            <Button Grid.Column="2" Panel.ZIndex="1" Style="{StaticResource CloseButton}" Click="CloseButton"/>
            <Image Grid.Column="2" Panel.ZIndex="2" Source="/Resource/Close.png" Margin="15,13,15,13" IsHitTestVisible="False"/>
            
        </Grid>

    </Grid>
</UserControl>
