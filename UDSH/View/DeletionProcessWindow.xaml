<!-- Copyright (C) 2025 Mohammed Kenawy -->
<Window x:Class="UDSH.View.DeletionProcessWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UDSH.View"
        mc:Ignorable="d"
        Title="DeletionProcessWindow" Height="150" BorderBrush="#434343" BorderThickness="4" Width="500" Background="Transparent" AllowsTransparency="True" WindowStartupLocation="CenterOwner" WindowStyle="None" ShowInTaskbar="False">

    <Grid Background="{StaticResource NewFileProcessBackgroundSCB}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Style="{StaticResource DefaultText}" Text="Deletion Process" Margin="10,0,0,0" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Style="{StaticResource DefaultText}" Text="Do You Want '"/>
            <TextBlock x:Name="ItemTextBlock" Style="{StaticResource DefaultText}"/>
            <TextBlock Style="{StaticResource DefaultText}" Text="' Deleted?"/>
        </StackPanel>
    </Grid>

</Window>
